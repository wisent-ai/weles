[package]
name = "skarbiec-entitlements-router"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "skarbiec-entitlements-router"
path = "src/skarbiec/main.rs"

[dependencies]
anyhow = "1.0"
serde_json = "1.0"
chrono = { version = "0.4", default-features = false, features = ["clock", "std"] }
libc = "0.2"
rand = "0.8"
zeroize = "1.8"
