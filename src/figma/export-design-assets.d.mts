/**
 * The exporter runs on import: it acquires the Figma credential, exports every
 * declared file and publishes the result, then prints its report. There is
 * nothing to call, so the module has no exports.
 */
export {};
